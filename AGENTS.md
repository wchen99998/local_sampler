We use uv, python 3.12, flax 0.12.1, jax 0.8.1.
For coding, prefer pure functional style with minimal boilerplate.
Use context7 mcp to search for code documentations if necessary
Use inline python to test and probe a given library if can't find docs
