[project]
name = "locally-tilted-sampler"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flax==0.12.1",
    "jax==0.8.1",
    "jaxlib==0.8.1",
    "optax==0.2.6",
    "matplotlib>=3.8.0",
]
